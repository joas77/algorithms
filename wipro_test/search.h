#ifndef SEARCH_H
#define SEARCH_H


/*
 * Prints status of algorithm and
 * search for val in a sorted array A[] and returns
 * the index of first ocurrence of val in A
 * Returns -1 if val is not found
 * */
int binSearch(int A[], int n, int val);

#endif /*SEARCH_H*/
